package com.example.tiodaperua.ui.login

import android.os.Bundle
import androidx.appcompat.app.AppCompatActivity

class ResponsavelRegisterSimplifiedActivity : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        // Esta tela não é mais utilizada e seu conteúdo foi removido para corrigir erros de compilação.
        // O arquivo pode ser deletado do projeto.
    }
}
